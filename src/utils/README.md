# utils

工具函数目录，包含用于序列化和处理插件特性的辅助函数。

## 模块

### `serialize-feature.ts`

序列化功能函数，用于将功能对象转换为可序列化的 JSON 格式。

**功能：**
- 移除功能对象中的函数类型属性
- 保留所有非函数属性（包括嵌套对象和数组）
- 返回符合 `JsonValue` 类型的对象

**使用场景：**
- 在注册表中序列化功能定义，以便通过网络传输
- 将包含执行函数的功能定义转换为纯数据格式

**相关模块：**
- 被 `src/registry.ts` 使用，用于序列化已注册的功能
